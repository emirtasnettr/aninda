DATABASE_URL="postgresql://postgres:postgres@localhost:5432/teslimatjet?schema=public"
JWT_SECRET="change-me-in-production-use-long-random-string"
JWT_EXPIRES_SEC=604800
# Nest API (courier/customer varsayılanları localhost:3000 kullanır)
PORT=3000
# Kurye ödeme talebi minimum tutarı (₺). Boşsa 100.
# PAYOUT_MIN_TRY=100
# Admin paneli: admin-panel içinde `npm run dev` → 3001 (çakışmayı önlemek için)
